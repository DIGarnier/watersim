/target

# Generated render outputs (reproducible via `render --webp` / the default gif
# mode). The tracked `renders/settle.png` is the one committed sample.
/renders/*.webp
/renders/*.gif
/renders/*_still.png
